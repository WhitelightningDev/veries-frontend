{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "npm run build:pages",
  "outputDirectory": "dist/client",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
